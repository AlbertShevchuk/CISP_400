int main()
{
ifstream in("row.txt");
if (!in)
{
cout <<"error: ifstream 'in' is not open'";
	cin.get();
}
in.close()
in.open("Row.txt")
}

in.clear();
in.close();
cin>>c;


while((c = in.get()) != EOF )

while(in >> s)

while (in >> s , (( c = in.get()) != EOF ))

while((in.getline(s,SIZE)))

while((in.getline(s,SIZE, EOF))) //will read in one loop
 
in.close();
cin>>c;
